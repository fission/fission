\* TLC config for aliasgc.tla — the guarded (correct) design.
\* Run: java -jar tla2tools.jar -deadlock -config aliasgc.cfg aliasgc.tla
SPECIFICATION Spec
CONSTANTS
  Versions = {v1, v2}
  NoOne = NoOne
  RecheckGuard = TRUE
INVARIANTS
  TypeOK
  NoDanglingAlias
