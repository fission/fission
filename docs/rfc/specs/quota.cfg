\* TLC config for quota.tla — the atomic (correct) design.
\* Run: java -jar tla2tools.jar -deadlock -config quota.cfg quota.tla
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2}
  MaxKeys = 1
  AtomicQuota = TRUE
INVARIANTS
  TypeOK
  QuotaNeverExceeded
  CommitsOnce
