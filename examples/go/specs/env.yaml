apiVersion: fission.io/v1
kind: Environment
metadata:
  name: go
  namespace: default
spec:
  version: 2
  builder:
    command: build
    image: fission/go-builder:0.6.0
  runtime:
    image: fission/go-env:0.6.0

