# Binaries
cmd/fission-bundle/fission-bundle
cmd/fission-cli/fission
cmd/fission-cli/fission-cli
cmd/fetcher/fetcher
cmd/builder/builder
cmd/preupgradechecks/pre-upgrade-checks
cmd/reporter/reporter

# Logs
test/logs/
test/e2e/cli/fission-dump/
test/integration/logs/
test/integration/suites/*/test/

# Pycharm IDE
.idea
environments/php7/vendor/

# terraform files
.terraform/
*.tfstate
*.backup

# Common backup files
*.swp
*.bak
*.tmp
*~
*.tgz

.DS_Store
vendor/
local/

build/
# Output of `make generate-webhooks` — see comments on the Makefile target.
bin/
dist/
manifest/
.vscode/
coverage.txt

cosign.key

# Dumps
.dumps/

# SAST scan artefacts (SARIF reports)
output.sarif
*.sarif
/fission-bundle

# Stray archive-download artifacts from e2e CLI test runs (UUID-named files
# written to the test's working directory by `fission archive download`).
test/e2e/cli/????????-????-????-????-????????????
# Added by code-review-graph
.code-review-graph/
notes
examples
.nvim.json
.github/code-review-graph.instruction.md
.claude/skills
.claude/settings.json
.mcp.json
.envrc
common.log
kind-132.yaml

# Maven build output from the JVM testdata trees. `ZipTestDataTree` ships the
# whole source tree to the builder, which runs mvn itself — a locally-built
# target/ is both redundant and easy to commit by accident.
test/integration/testdata/**/target/
