apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: executor-monitor
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      svc: executor
  endpoints:
  - targetPort: 8080