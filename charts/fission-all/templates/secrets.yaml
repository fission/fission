apiVersion: v1
kind: Secret
metadata:
  name: influxdb
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
type: Opaque
data:
  username: {{ .Values.loggerInfluxdbAdmin | b64enc | quote }}
  password: {{ randAlphaNum 20 | b64enc | quote }}