apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: controller-monitor
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      svc: controller
  endpoints:
  - targetPort: 8080