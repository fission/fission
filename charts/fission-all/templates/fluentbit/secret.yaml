apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-fission-fluentbit-secret
type: Opaque
data:
  {{- if .Values.logger.fluentbit.env.sensitive }}
  {{- range $key, $val := .Values.logger.fluentbit.env.sensitive }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
  {{- end}}