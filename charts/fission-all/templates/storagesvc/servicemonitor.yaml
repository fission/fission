apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: storagesvc-monitor
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      svc: storagesvc
  endpoints:
  - targetPort: 8080