apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: router-monitor
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      svc: router
  endpoints:
  - targetPort: 8080