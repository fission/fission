{{- if .Values.mcp.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fission-mcp
  namespace: {{ .Release.Namespace }}
  {{- include "fission.saMetadata" (dict "root" . "component" "mcp") }}
{{- end }}
