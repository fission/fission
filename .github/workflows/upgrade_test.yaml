name: Upgrade functionality test

on: 
  push:
    branches: [ upgrade-testing ]

jobs:
  
  upgrade-functionality-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout action sources
      uses: actions/checkout@v2.3.4

    - name: Checkout fission sources
      uses: actions/checkout@v2.3.4
      with:
        repository: fission/examples
        path: examples

    - name: Setup go
      uses: actions/setup-go@v2.1.3
      with:
        go-version: '1.14.15'

    - name: Setup Helm
      uses: Azure/setup-helm@v1
      with:
        version: v3.3.4
    
    - name: Update helm
      run: helm repo add helm https://charts.helm.sh/stable 

    - name: Setup Skaffold 
      run: |
        curl -Lo skaffold https://storage.googleapis.com/skaffold/releases/latest/skaffold-linux-amd64
        sudo install skaffold /usr/local/bin/
        skaffold version

    - name: Setup Kind Clutser
      uses: engineerd/setup-kind@v0.5.0
      with:
        config: kind.yaml
        
    - name: Setup kubectl & fetch node information
      run: |
        kubectl cluster-info --context kind-kind
        kind get kubeconfig --internal >$HOME/.kube/config
        kubectl get nodes
        sudo apt-get install -y apache2-utils
           
#    - name: Build and install fission stable version from upstream repo 
#      run: |
#        export FISSION_NAMESPACE="fission"
#        kubectl create namespace $FISSION_NAMESPACE
#        helm install --namespace $FISSION_NAMESPACE --name-template fission https://github.com/fission/fission/releases/download/1.12.0/fission-all-1.12.0.tgz

#    - name: Install fission cli stable version from upstream
#      run: |
#        sleep 10
#        curl -Lo fission https://github.com/fission/fission/releases/download/1.12.0/fission-cli-linux && chmod +x fission && sudo mv fission /usr/local/bin/
#        fission version
#        sleep 2
#        fission env create --name nodejs --image fission/node-env:latest
#        curl -LO https://raw.githubusercontent.com/fission/examples/master/nodejs/hello.js
#        fission function create --name hello --env nodejs --code hello.js
#        sleep 5
#        fission function test --name hello
#        sleep 10

    - name: Build and install latest fission
      run: |
        kubectl create namespace fission
        skaffold run -p kind-ci
        echo "waiting for 60 sec......"
        sleep 60
        kubectl get pods -A
#        fission version


    - name: Build and install latest fission CLI
      run: |
        /bin/bash ./test/upgrade_test/fission_cli.sh